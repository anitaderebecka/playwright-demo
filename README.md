# Demo repo tests in Playwright

This repository contains a small set of Playwright tests demonstrating app building and project structure.

I'm using the Page Object Model (POM) pattern and striving to follow SOLID principles during development.

**Important:** The system under test utilizes Server-Side Rendering (SSR).
