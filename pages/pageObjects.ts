export enum DomRole {
  button = 'button',
  dialog = 'dialog',
  link = 'link',
  textbox = 'textbox'
}

export enum CommunicationLocator {
  error = '.error'
}

export enum State {
  visible = 'visible',
  hidden = 'hidden'
}
